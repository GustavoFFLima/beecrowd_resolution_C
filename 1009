#include <stdio.h>

int main()
{
    char name[21];
    double salary, saleTotal, Total;
    scanf("%s %lf %lf", &name, &salary, &saleTotal);
    Total = salary + (saleTotal * 0.15);
    printf("TOTAL = R$ %.2lf\n", Total);
    return 0;
}
