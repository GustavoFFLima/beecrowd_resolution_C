#include <stdio.h>
#include <stdlib.h>

int main()
{
    int  R;
    const double pi = 3.14159;
    double VOLUME;
    scanf("%d", &R);
    VOLUME = (4/3.0) * pi * R * R * R;
    printf("VOLUME = %.3lf\n", VOLUME);
    return 0;
}
